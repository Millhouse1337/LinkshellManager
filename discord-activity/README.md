# Discord Activity Frontend

The Angular Activity lives in this folder and builds into `../wwwroot/discord-activity`.

Primary workflow:

```bash
npm install
npm run build
```

The built files are served by the ASP.NET Core host at `/discord-activity`.

See the repo root [README](../README.md) for the full architecture, auth flow, local setup, Discord Developer Portal steps, and deployment notes.
